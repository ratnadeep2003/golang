package main

import "fmt"

func main() {
	y := uint(0)
	fmt.Printf("%T",y) //f = format of datatype: y=uint
	fmt.Println(y); //ln = adds a new line at the end
	fmt.Print(y); //print = also prints value of y, but no new line
}
